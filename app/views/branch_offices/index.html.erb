<p id="notice"><%= notice %></p>

<h1>Branch Offices</h1>

<table>
  <thead>
    <tr>
      <th>Address</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th>Partner</th>
      <th>Email</th>
      <th>Phone</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @branch_offices.each do |branch_office| %>
      <tr>
        <td><%= branch_office.address %></td>
        <td><%= branch_office.latitude %></td>
        <td><%= branch_office.longitude %></td>
        <td><%= branch_office.partner_id %></td>
        <td><%= branch_office.email %></td>
        <td><%= branch_office.phone %></td>
        <td><%= link_to 'Show', branch_office %></td>
        <td><%= link_to 'Edit', edit_branch_office_path(branch_office) %></td>
        <td><%= link_to 'Destroy', branch_office, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Branch Office', new_branch_office_path %>
