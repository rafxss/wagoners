<div class="container">
  <br>

  <h1 class="text-center">Regístrate</h1>
  <br>

  <p class="text-center px-4">Crea una cuenta en BBVoucher y empieza a disfrutar de ofertas personalizadas.</p>
  <br>

  <div data-controller="autocomplete">
    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs">
        <div class="form-row">
              <div class="col text-left">
                <%= f.input :first_name,
                            required: true,
                            label: false,
                            placeholder: 'Nombre*',
                            input_html: { class: 'form-input-wagoners' } %>
              </div>
              <div class="col text-left">
                <%= f.input :last_name,
                            label: false,
                            placeholder: 'Apellidos',
                            input_html: { class: 'form-input-wagoners' } %>
              </div>
        </div>

        <%= f.input :address, required: true, label: false, placeholder: 'Dirección*',input_html: { class: 'form-input-wagoners', data: { target: 'autocomplete.input'} } %>
        <%#= f.input :date_of_birth, required: true, class: "form-control" %>
        <%= f.input :geolocation_access, label: 'Acepto que se comparta mi ubicación con BBVouchers', label_html: {class: "terms"} %>
        <br>
        <%= f.input :date_of_birth, required: true, label: "Fecha de nacimiento",
                    start_year: Date.today.year - 90, end_year: Date.today.year - 12,
                    order: [:day, :month, :year] %>

        <%= f.input :email,
                    required: true,
                    autofocus: true,
                    label: false,
                    placeholder: 'Email*',
                    input_html: { autocomplete: "email", class: 'form-input-wagoners' } %>
        <%= f.input :password,
                    required: true,
                    label: false,
                    placeholder: 'Contraseña*',
                    input_html: { autocomplete: "new-password", class: 'form-input-wagoners' } %>
        <%= f.input :password_confirmation,
                    required: true,
                    placeholder: 'Repita la contraseña*',
                    label: false,
                    input_html: { autocomplete: "new-password", class: 'form-input-wagoners' } %>
      </div>
      <br>

      <div class="form-actions d-flex">
        <%= f.button :submit, "Sign up", class: "btn-bbva-primary flex-grow-1 py-2" %>
      </div>
    <% end %>
  </div>
  <br>

  <div class="text-center mb-5">
    <a class="link-form" href="/users/sign_in">¿Ya tienes cuenta? Inicia sesión</a>
  </div>
  <br>
  <span class="mb-5">‎</span>


</div>
