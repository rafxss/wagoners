<div class="container">
<br>

  <h1 class="text-center">Inicia sesión</h1>
  <br>
  <p class="text-center px-4">Inicia sesión en tu cuenta de BBVoucher y continua disfruntando de ofertas personalizadas solo para ti.</p>

  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs">
      <%= f.input :email,
                  required: false,
                  autofocus: false,
                  label: false,
                  placeholder: 'Email',
                  input_html: { autocomplete: "email", class: 'form-input-wagoners' } %>
      <%= f.input :password,
                  required: false,
                  placeholder: 'Contraseña',
                  label: false,
                  input_html: { autocomplete: "current-password", class: 'form-input-wagoners' } %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Log in" %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>
